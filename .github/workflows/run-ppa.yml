name: Update PPA Packages

on:
  workflow_dispatch:
    inputs:
      package:
        description: 'Package to upload (cliphist, matugen, niri, niri-git, quickshell, quickshell-git, xwayland-satellite, xwayland-satellite-git, danksearch, dgop, or all)'
        required: false
        default: 'auto'
      force_upload:
        description: 'Force upload without version check'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
      rebuild_release:
        description: 'Release number for rebuilds (e.g., 2, 3, 4 for ppa2, ppa3, ppa4)'
        required: false
        default: ''
  # schedule:
  #   - cron: '0 */6 * * *'  # Every 6 hours (00:00, 06:00, 12:00, 18:00 UTC)

jobs:
  check-updates:
    name: Check for package updates
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    outputs:
      has_updates: ${{ steps.check.outputs.has_updates }}
      updated_packages: ${{ steps.check.outputs.updated_packages }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl git
      
      - name: Check for updates
        id: check
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x distro/scripts/*.sh
          
          # Check for updates (PPA uses distro/ubuntu by default)
          UPDATED=$(distro/scripts/debian-update.sh 2>&1 | tee /dev/stderr | tail -1)
          
          if [ -n "$UPDATED" ] && [ "$UPDATED" != " " ]; then
            echo "has_updates=true" >> $GITHUB_OUTPUT
            # Convert space-separated to comma-separated for consistency
            UPDATED_CSV=$(echo "$UPDATED" | tr ' ' ',')
            echo "updated_packages=$UPDATED_CSV" >> $GITHUB_OUTPUT
            echo "📋 Updates found: $UPDATED"
          else
            echo "has_updates=false" >> $GITHUB_OUTPUT
            echo "updated_packages=" >> $GITHUB_OUTPUT
            echo "📋 No updates found"
          fi

  upload-ppa:
    name: Upload to PPA
    needs: check-updates
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: |
      github.event.inputs.force_upload == 'true' ||
      (needs.check-updates.outputs.has_updates == 'true' && needs.check-updates.outputs.updated_packages != '')
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            debhelper \
            devscripts \
            dput \
            lftp \
            build-essential \
            fakeroot \
            dpkg-dev
      
      - name: Configure GPG
        env:
          GPG_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
        run: |
          echo "$GPG_KEY" | gpg --import
          GPG_KEY_ID=$(gpg --list-secret-keys --keyid-format LONG | grep sec | awk '{print $2}' | cut -d'/' -f2)
          echo "DEBSIGN_KEYID=$GPG_KEY_ID" >> $GITHUB_ENV
      
      - name: Determine packages to upload
        id: packages
        run: |
          if [[ "${{ github.event.inputs.force_upload }}" == "true" ]]; then
            PKG="${{ github.event.inputs.package }}"
            if [[ "$PKG" == "auto" ]]; then
              PACKAGES="cliphist matugen niri niri-git quickshell quickshell-git xwayland-satellite xwayland-satellite-git danksearch dgop"
            else
              PACKAGES="$PKG"
            fi
            echo "packages=$PACKAGES" >> $GITHUB_OUTPUT
            echo "🚀 Force upload: $PACKAGES"
          elif [[ -n "${{ github.event.inputs.package }}" && "${{ github.event.inputs.package }}" != "auto" ]]; then
            PACKAGES="${{ github.event.inputs.package }}"
            echo "packages=$PACKAGES" >> $GITHUB_OUTPUT
            echo "📦 Manual selection: $PACKAGES"
          elif [[ "${{ needs.check-updates.outputs.has_updates }}" == "true" ]]; then
            # Upload only updated packages
            UPDATED="${{ needs.check-updates.outputs.updated_packages }}"
            if [[ -n "$UPDATED" ]]; then
              # Convert comma-separated to space-separated
              PACKAGES=$(echo "$UPDATED" | tr ',' ' ')
              echo "packages=$PACKAGES" >> $GITHUB_OUTPUT
              echo "✨ Uploading updated packages: $PACKAGES"
            else
              PACKAGES="cliphist matugen niri niri-git quickshell quickshell-git xwayland-satellite xwayland-satellite-git danksearch dgop"
              echo "packages=$PACKAGES" >> $GITHUB_OUTPUT
            fi
          else
            PACKAGES="cliphist matugen niri niri-git quickshell quickshell-git xwayland-satellite xwayland-satellite-git danksearch dgop"
            echo "packages=$PACKAGES" >> $GITHUB_OUTPUT
          fi
      
      - name: Upload to PPA
        env:
          REBUILD_RELEASE: ${{ github.event.inputs.rebuild_release }}
        run: |
          PACKAGES="${{ steps.packages.outputs.packages }}"
          
          for pkg in $PACKAGES; do
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "Uploading $pkg to PPA..."
            if [ -n "$REBUILD_RELEASE" ]; then
              echo "🔄 Using rebuild release number: ppa$REBUILD_RELEASE"
            fi
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            bash distro/scripts/ppa-upload.sh "distro/ubuntu/$pkg" danklinux questing || echo "⚠️ Failed to upload $pkg"
          done
      
      - name: Commit changelog updates
        run: |
          # Check if there are any changelog changes to commit
          if git diff --exit-code distro/ubuntu/ >/dev/null 2>&1; then
            echo "📋 No changelog changes to commit"
          else
            # Get list of changed packages for commit message
            CHANGED=$(git diff --name-only distro/ubuntu/ | grep 'debian/changelog' | xargs dirname | xargs -I{} basename {} | tr '\n' ', ' | sed 's/,$//')
            
            echo "📋 Committing changelog updates for: $CHANGED"
            
            git add distro/ubuntu/
            git commit -m "ci: Auto-update PPA packages [$CHANGED]

            🤖 Automated by GitHub Actions"
            
            git push
          fi
      
      - name: Summary
        run: |
          echo "### PPA Package Upload Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [[ "${{ needs.check-updates.outputs.has_updates }}" == "true" ]]; then
            echo "- **Updated Packages**: ${{ needs.check-updates.outputs.updated_packages }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Uploaded Packages**: ${{ steps.packages.outputs.packages }}" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "- **PPA**: https://launchpad.net/~avengemedia/+archive/ubuntu/danklinux/+packages" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Builds will appear once Launchpad processes the uploads." >> $GITHUB_STEP_SUMMARY
